name: nuremberg-ci

on:
  workflow_dispatch:
  push:
    branches:
      - ci--setup-job-run-specs

  pull_request:
    types:
      - closed
    branches:
     - ci--setup-job-run-specs

jobs:
  Run:

    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      max-parallel: 4
      fail-fast: false

    steps:
        - name: Checkout code
          uses: actions/checkout@v3
        - uses: extractions/setup-just@v1
          with:
            just-version: '1.9.0'
        - name: Current Version
          run: just banner version
        - name: Build Test Image
          uses: ./.github/workflows/actions/build-tester.yml
